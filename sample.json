{
  "people": {
    "db": "people",
    "item": {
      "get": {
        "data": {
          "name": "{{ row.forename }}",
          "age": "{{ row.age }}"
        },
        "types": {
          "name": "string",
          "age": "int"
        },
        "query": {
          "op": "SELECT * FROM people WHERE id=%s",
	  "params": ["{{ uri.tag }}"]
	}
      }
  },
  "toys": {},
  "orders": {}
  }
}

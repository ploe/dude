{
  "people": {
    "db": "people",
    "item": {
      "get": {
        "data": {
          "name": "{{ this.name }}",
          "age": "{{ this.age }}",
          "bio": "{{ this.name }} likes {{ this.hobbies }}"
        },
        "types": {
          "name": "string",
          "age": "int"
        },
        "query": {
          "op": "SELECT * FROM people WHERE id=%s",
	  "params": ["{{ uri.tag }}"]
	}
      }
  },
  "toys": {},
  "orders": {}
  }
}
